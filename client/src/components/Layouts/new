import React, { Fragment, Component } from "react"
import { Field, reduxForm } from "redux-form"
import TextField from "@material-ui/core/TextField"
import { makeStyles } from "@material-ui/core/styles"

class BranchForm extends Component {
  useStyles = makeStyles(theme => ({
    container: {
      display: "flex",
      flexWrap: "wrap"
    },
    textField: {
      marginLeft: theme.spacing(1),
      marginRight: theme.spacing(1),
      width: 600
    }
  }))

  textFieldComponent = ({ input }) => {
    const classes = this.useStyles()
    console.log(input)
    return <input {...input} className={classes.textField} />
  }
  render() {
    return (
      <form>
        <Field
          name="Branch"
          label="Enter Branch"
          component={this.textFieldComponent}
        ></Field>
        <Field
          name="Branchesss"
          label="Enter Branches"
          component={this.textFieldComponent}
        ></Field>
      </form>
    )
  }
}

const validate = formValues => {
  console.log(formValues)
}

export default reduxForm({ form: "branchForm", validate })(BranchForm)





className={classes.textField}


import React, { Component } from "react"
import { Field, reduxForm } from "redux-form"

class BranchForm extends Component {
  textFieldComponent = ({ input }) => {
    console.log(input)
    return <input {...input} />
  }

  render() {
    return (
      <form>
        <Field name="Branch" label="Enter Branch" component="input"></Field>
      </form>
    )
  }
}

export default reduxForm({ form: "branchForm" })(BranchForm)
